# Core dependencies
selenium==4.15.2
requests==2.31.0
wget==3.2
zipfile36==0.1.3
urllib3==2.0.7

# FastAPI stack
fastapi==0.103.1
uvicorn==0.23.2

# System-level (no pip install needed - included for clarity)
# os, stat, json -> Python built-ins
# urllib.parse -> Python built-in

# WebDriver management alternatives (better than wget)
webdriver-manager==3.8.6  # Recommended for ChromeDriver auto-download